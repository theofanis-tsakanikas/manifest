"""The acceptance gates — one module per claim, plus the structural ones.

A gate is a refusal, not a report. Each ships with the mutation in `scripts/gate_proof.py`
that breaks it, in the same commit, because a gate that has never been shown to fail is a
comment.
"""
