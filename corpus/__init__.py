"""The corpus generator, its ground truth and its declared operating envelope.

Not part of the installed package: a document generator has no business shipping in a wheel.
It is importable by the suite because `conftest.py` puts the repository root on `sys.path`.
"""
